# Expo/React Native + Metro resolve some transitive deps (metro-runtime, @babel/runtime,
# etc.) by walking node_modules, which pnpm's default isolated layout hides. Hoisting to
# the root node_modules is Expo's recommended setup for pnpm monorepos.
shamefully-hoist=true
